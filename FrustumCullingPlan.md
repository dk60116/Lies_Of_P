# Frustum Culling 구현 작업 순서

## 가능 여부
카메라 절두체(Frustum) + 옥트리(Octree) + MeshBuffer의 BoundingBox 조합으로 프러스텀 컬링 구현이 가능합니다.
현재 코드에도 카메라 View/Projection 계산과 MeshBuffer의 로컬 BoundingBox 데이터가 있어, 월드 AABB 계산과 절두체 교차 판정 루틴을 추가하면 단계적으로 적용할 수 있습니다.

## 작업 목록
1. 카메라 절두체 데이터 구조를 추가한다.
   - `CCamera`의 View/Projection 행렬에서 6개 평면(Left/Right/Top/Bottom/Near/Far)을 추출하는 API를 정의한다.
   - 카메라가 `Update()`에서 행렬을 갱신한 직후 절두체 평면도 함께 갱신하도록 연결한다.

2. BoundingBox 좌표계를 통일한다.
   - `CMeshBuffer::MESHBUFFERDESC::boundingBox`는 로컬 공간 데이터이므로, 렌더러 기준으로 월드 AABB를 계산하는 유틸을 만든다.
   - 스키닝 메시의 경우 이미 사용 중인 애니메이션 월드 바운드 계산 경로를 재사용해 동적 바운드를 얻는다.

3. 옥트리 노드/엔트리 포맷을 설계한다.
   - 노드에는 월드 AABB, 자식 포인터(최대 8개), 포함 렌더러 목록을 둔다.
   - 엔트리는 최소한 `CRenderer*`와 월드 AABB를 유지하도록 한다.

4. 씬 단위 옥트리 빌드를 추가한다.
   - 씬 로딩 완료 시 정적 오브젝트를 기준으로 초기 옥트리를 빌드한다.
   - 루트 영역은 씬 전체 AABB 또는 환경 설정 기반 고정 크기로 결정한다.

5. 동적 오브젝트 갱신 정책을 정한다.
   - 매 프레임 이동 가능 오브젝트는 별도 동적 리스트로 유지하거나, dirty 플래그 기반으로 옥트리 재삽입한다.
   - 정적/동적 분리 정책을 기존 `Add_RenderTarget_Mesh`의 static/dynamic 분류와 맞춘다.

6. 절두체 vs 옥트리 노드 판정을 구현한다.
   - 노드 AABB가 절두체 밖이면 하위 전체를 스킵한다.
   - 노드가 절두체와 교차/포함이면 자식 노드 또는 엔트리를 계속 순회한다.

7. 엔트리 단위 2차 판정을 적용한다.
   - 최종 후보 렌더러의 월드 AABB와 절두체를 다시 검사해 false positive를 줄인다.
   - 통과한 렌더러만 카메라 렌더 큐에 넣는다.

8. 카메라 렌더 파이프라인에 통합한다.
   - 현재는 `RenderMesh()`가 리스트 전체를 순회하므로, 컬링 결과 리스트를 받아 순회하도록 경로를 교체한다.
   - 에디터 카메라/게임 카메라 모두 동일한 컬링 경로를 사용하도록 일원화한다.

9. 디버그 시각화를 추가한다.
   - 선택된 메시 바운딩박스를 그리는 기존 디버그 루틴처럼, 절두체와 옥트리 노드 경계를 옵션으로 렌더링한다.
   - 컬링된 수, 렌더링된 수, 트래버스한 노드 수를 프레임 통계로 노출한다.

10. 검증 및 튜닝을 수행한다.
    - 카메라 이동/회전, 대규모 씬, 스키닝 메시 포함 씬에서 팝인/팝아웃 오류를 확인한다.
    - 옥트리 최대 깊이, 노드당 엔트리 임계값, 동적 재삽입 빈도를 조정해 CPU 비용과 컬링 효율을 균형화한다.
